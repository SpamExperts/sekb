Encryption TLS
==============

The SpamExperts system fully supports incoming connections protected
using TLS. Deliveries are always made over TLS when supported by the
destination mailserver. This way all email is securely transmitted.
